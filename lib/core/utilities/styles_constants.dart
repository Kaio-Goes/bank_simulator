import 'package:flutter/material.dart';

const iconMenu = Icon(
  Icons.menu,
  size: 35,
  color: Colors.white,
);

const iconNotification = Icon(
  Icons.notification_add_outlined,
  size: 35,
  color: Colors.white,
);
